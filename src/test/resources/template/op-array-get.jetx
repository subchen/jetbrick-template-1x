#define (String[] items)
#set (List<String> list = ["a","b","c","d"])
#set (int i=1)
${list[0]}
${list[i+1]}
${items[0]}
${items[i+1]}
#set (map = {"id":1, "name":"chen"})
${map["id"]}
${map[name]}
#set (User u = context["user"])
${u.name}